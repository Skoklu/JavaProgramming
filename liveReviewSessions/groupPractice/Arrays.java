package groupPractice;

public class Arrays {

    /*
    package Practice;

import java.util.ArrayList;
import java.util.Arrays;

public class PrimitiveToNonPrimitive {
    public static void main(String[] args) {


        // primitive array
        int[] num1 = {1,2,3,4,5,6,7};    // outer array
        int [] num2 = {3,5,6,7,8};       // inner array

        System.out.println(Arrays.toString(num1));
        System.out.println(Arrays.toString(num2));


        System.out.println("============================================");

        // convert to Integer Array and determine the size of Integer Array

        // primitive to non-primitive Array

        Integer [] numbers1 = new Integer[num1.length];
        Integer [] numbers2 = new Integer[num2.length];


        System.out.println(Arrays.toString(numbers1));
        System.out.println(Arrays.toString(numbers2));

        // add element to Non-primitive Array (outer)

        int j=0;
        for (int each : num1) {
            numbers1 [j++] = each;
        }

        System.out.println(Arrays.toString(numbers1));

        // add element to Non-primitive Array (inner)

        int k = 0;
        for (int each : num2) {
            numbers2[k++] = each;
        }

        System.out.println(Arrays.toString(numbers2));


        System.out.println("=============================================");

        // creating Array list using Non-primitive array

        ArrayList<Integer> numbersArraylist1 = new ArrayList<>(Arrays.asList(numbers1));
        ArrayList<Integer> numbersArrayList2 = new ArrayList<>(Arrays.asList(numbers2));


        System.out.println(numbersArraylist1);
        System.out.println(numbersArrayList2);

        // check the array list and find if the inner loop is inside the outer loop or not.
         It returns boolean result


        boolean r1 = numbersArraylist1.containsAll(numbersArrayList2);

        System.out.println(r1);








     */
}
